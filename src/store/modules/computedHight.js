const state = {
    HEIGHT : 0,
};

const getters = {

};

const actions = {

};
 const mutations = {
     COMPUTED_HEIGHT : (state ,msg) => {
         state.HEIGHT = msg;
     }
 };

 export default {
     state,
     getters,
     mutations,
     actions
 }