const state = {
  customer:[]
};
const getters = {
//留着备用
};
const actions = {
//留着备用

};
const mutations = {
  customer:(state,msg)=>{
    state.customer = msg;
  }
};
export default {
  state,
  getters,
  actions,
  mutations
}
