const state = {
    egg: [],
};

const getters = {

};

const actions = {

};
const mutations = {
    COMPUTED_EGG: (state, msg) => {
        state.HEIGHT = msg;
    }
};

export default {
    state,
    getters,
    mutations,
    actions
}